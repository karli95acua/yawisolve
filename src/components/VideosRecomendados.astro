---
import { getVideos } from '../services/api';

interface Video {
    id: number;
    titulo: string;
    url_s3: string;
    modulo_id: number;
}

async function getRandomVideos(count: number): Promise<Video[]> {
    const videos = await getVideos();
    const shuffled = [...videos].sort(() => 0.5 - Math.random());
    return shuffled.slice(0, count);
}

const recommendedVideos: Video[] = await getRandomVideos(6);
---

<section class="bg-slate-100 py-8">
    <h2 class="text-4xl font-semibold text-center text-slate-800 mb-8">Recomendación de Tutoriales</h2>
    <div class="mx-auto max-w-8xl px-20">
        <div class="grid grid-cols-2 sm:grid-cols-3 gap-8">
            {recommendedVideos.map((video) => (
                <a href={`/videos/${video.id}`} class="group relative block overflow-hidden rounded-2xl bg-white shadow-md transition-all hover:scale-[1.05] hover:shadow-xl">
                    <div class="w-full h-80 overflow-hidden">
                        <video controls class="w-full h-full object-cover">
                            <source src={video.url_s3} type="video/mp4" />
                        </video>
                    </div>
                    <div class="absolute inset-x-0 bottom-0 h-2/3 bg-gradient-to-b from-transparent via-black/80 to-black/95 p-12 pt-20 text-white flex flex-col justify-end">
                        <h3 class="text-2xl text-slate-200 font-bold mb-2">{video.titulo}</h3>
                    </div>
                </a>
            ))}
        </div>
    </div>
</section>


